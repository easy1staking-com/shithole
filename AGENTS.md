# AGENTS.md

Project instructions for this repository live in **[`CLAUDE.md`](CLAUDE.md)** — the
single canonical source (purpose, constitution, commands, contract change checklist,
factory directive). Read it in full before doing anything; this file intentionally
carries no copy, so the two cannot drift.

Codex-specific: the permission policy for this checkout is generated locally into
`.codex/` from the factory manifest; it is not part of the repository.
